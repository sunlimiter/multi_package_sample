name: network
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  dio: ^5.9.2
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  injectable: ^2.7.1+4
  session: 0.0.1

dev_dependencies:
  build_runner: ^2.11.1
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.9.1

flutter:
  assets:
    - assets/mock/